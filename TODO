Binary output is empty
Fix regression tests that fail the unit tests
create an eqtl struct
Make bed output another SnarlAnalyzer class
    Take out the duplicate copy of make_matrix() that I added so bed making would work
Other TODOs in snarl_analyzer
Double check that there aren't functions defined that never get used
move writeSignificantTableToTSV to writer.hpp
Split main file into two files
Combine regression code
Add functional tests
More unit tests
Identify ref paths if .chr file not present
Test in the snarl decompose paths if in the .chr chr name are present in the graph if not stop the analysis and return a error ("chr not found")
